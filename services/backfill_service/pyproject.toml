[project]
name = "backfill-service"
version = "0.1.0"
description = "Backfill Service - data gap detection and historical data filling"
requires-python = ">=3.11"
dependencies = [
    "algo-trader-shared",
    "yfinance>=0.2",
    "pandas>=2.0",
    "celery[redis]",
]
