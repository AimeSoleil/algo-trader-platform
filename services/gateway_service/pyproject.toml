[project]
name = "gateway-service"
version = "0.1.0"
description = "API Gateway - unified Swagger docs and reverse proxy for all services"
requires-python = ">=3.11"
dependencies = [
    "algo-trader-shared",
    "fastapi",
    "uvicorn[standard]",
    "httpx>=0.27",
]
